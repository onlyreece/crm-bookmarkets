{
  "name": "bookmarklets",
  "version": "1.0.3",
  "description": "",
  "main": "compile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-ci":"node compile.js",
    "build": "npm --no-git-tag-version version patch && node compile.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "bookmarklet": "0.0.6"
  }
}
